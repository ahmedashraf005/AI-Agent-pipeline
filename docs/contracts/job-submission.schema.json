{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JobSubmissionRequest",
  "description": "Payload sent from .NET Gateway API to the Python Agent Service to kick off processing.",
  "type": "object",
  "required": ["jobId", "text", "fileName"],
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "description": "Generated by the Gateway at ingestion time. Doubles as the idempotency key — if the same jobId is submitted twice, the agent service must return the existing result rather than reprocessing."
    },
    "text": {
      "type": "string",
      "description": "Extracted plain text of the uploaded document. Never raw binary."
    },
    "fileName": {
      "type": "string"
    }
  }
}
