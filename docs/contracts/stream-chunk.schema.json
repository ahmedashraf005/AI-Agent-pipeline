{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StreamChunk",
  "description": "A single SSE event sent from Python -> .NET -> React. One 'type' governs how the frontend should render it.",
  "type": "object",
  "required": ["jobId", "type", "content"],
  "properties": {
    "jobId": { "type": "string", "format": "uuid" },
    "type": {
      "type": "string",
      "enum": ["token", "status", "error"],
      "description": "token = a piece of generated text to append. status = a JobStatus transition (see job-status-transitions.md). error = terminal failure, frontend should stop listening."
    },
    "content": {
      "type": "string",
      "description": "For type=token: the text fragment. For type=status: one of the JobStatus enum values. For type=error: human-readable error message (never a stack trace)."
    },
    "iterationCount": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Optional. Present on status events emitted after the Auditor node runs, carrying the graph state's iteration_count so the Gateway can persist LoopIterations. Omitted or null for token/error events and status events before an iteration count is meaningful."
    }
  }
}
